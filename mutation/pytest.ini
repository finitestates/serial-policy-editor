[pytest]
testpaths = tests
pythonpath = src
markers =
    invariant: behavior that must remain stable across implementation changes
    current_workflow: behavior tied to the current application workflow or implementation
    optional: tests for optional packages, backends, or integrations
